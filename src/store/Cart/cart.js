// import { create } from "zustand";

// export const useOrderStore = create((set) => ({
//   cart: [],

//   addNewItem: (item) => set((state) => ({ cart: [...state.cart, item] })),

//   resetCart: () => set(() => ({ cart: [] })),
// }));
